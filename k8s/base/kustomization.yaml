apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: commonware-avs

resources:
  - namespace.yaml
  - rbac.yaml
  - configmap.yaml
  - pvc.yaml
  - ethereum/deployment.yaml
  - ethereum/service.yaml
  - eigenlayer/job.yaml
  - signer/deployment.yaml
  - signer/service.yaml
  - nodes/statefulset.yaml
  - nodes/service.yaml
  - router/deployment.yaml
  - router/service.yaml

commonLabels:
  app.kubernetes.io/part-of: commonware-avs
