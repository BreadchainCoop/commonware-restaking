apiVersion: v1
kind: Service
metadata:
  name: ethereum-svc
  namespace: commonware-avs
  labels:
    app: ethereum
spec:
  selector:
    app: ethereum
  ports:
    - name: rpc
      port: 8545
      targetPort: 8545
  type: ClusterIP
