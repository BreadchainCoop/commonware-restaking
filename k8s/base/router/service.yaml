apiVersion: v1
kind: Service
metadata:
  name: router-svc
  namespace: commonware-avs
  labels:
    app: router
spec:
  selector:
    app: router
  ports:
    - name: app
      port: 3000
      targetPort: 3000
    - name: ingress
      port: 8080
      targetPort: 8080
  type: ClusterIP
