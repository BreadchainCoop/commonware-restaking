apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nodes-data
  namespace: commonware-avs
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard
